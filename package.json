{
  "name": "ts-utils",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Some utils functions that I often use in my TS based projects",
  "license": "MIT",
  "repository": "kayno0o/ts-utils",
  "main": "./dist/index.mjs",
  "types": "./dist/types.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "build:tsc": "tsc",
    "lint:fix": "eslint . --fix"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dayjs": "^1.11.10",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.14.0",
    "@types/bun": "latest",
    "eslint": "^9.0.0",
    "eslint-plugin-format": "^0.1.0"
  }
}
